import type { ThemeMeta } from '../types'

const meta: ThemeMeta = {
  id: 'default',
  name: 'Default',
  description: 'Clean and minimal design with soft, warm tones. Light and dark mode support.',
}

export default meta
